# Журнал обходу інтерфейсу

Покроковий обхід міні-аппа: одна кнопка → скрін → правки → деплой.
Файл ведеться **під час** обходу: щойно екран пройдено — сюди пишеться,
що на ньому є і що змінили. Мета — більше не повертатися до вже пройденого.

**Позначки статусу:** ⬜ не дивилися · 🔎 в роботі · ✅ ок, змін не треба · 🔧 змінено й задеплоєно

---

## Як працюємо

Обхід іде **колами**. Одне коло — це прохід усіх екранів від початку до кінця;
після кола перезавантажуємо додаток і йдемо наступне. Так доки не буде ідеально.

Усередині кола:

1. Ти відкриваєш екран у Telegram → скидаєш скрін.
2. Я звіряю зі кодом, записую сюди що на екрані (кнопки, переходи, файл:рядки)
   і **сам кажу, що варто покращити**, навіть якщо ти не питав.
3. Кажеш що не так → правлю, `typecheck`, коміт, push одразу в `main`,
   Railway деплоїть сам (~1 хв).
4. **Додаток НЕ перезавантажуємо** — йдемо далі по старій збірці.
5. Ставлю статус екрана і переходимо до наступного.

Наприкінці кола: закрити міні-апп **повністю** (не згорнути) і відкрити знову —
Telegram WebView інакше віддасть стару збірку. Далі — наступне коло.

> **Важливо через це:** поки коло триває, скрін показує **стару** збірку. Тож у
> записах нижче розділяю «що було на скріні» і «що вже виправлено в коді, але
> ще не видно». Виправлене на скріні наступного кола має зникнути — якщо ні,
> це справжній баг, а не кеш.

Гілка розробки: `claude/project-by-description-h9lscb`, пуш одразу в `main`.

---

## Карта екранів (чек-лист)

### Верхній рівень — `App.tsx`

| # | Екран | Файл | Статус |
|---|-------|------|--------|
| 0 | Головне меню | `screens/Menu.tsx` | ⬜ |
| 1 | 🚚 Логістика | `screens/Logistics.tsx` (440 р.) | ⬜ |
| 2 | 🚗 Дорожній табель | `screens/RoadTimesheet.tsx` (~4600 р.) | 🔎 |
| 3 | 🧱 Матеріали | `screens/Materials.tsx` (260 р.) | ⬜ |
| 4 | 📊 Статистика | `screens/Stats.tsx` (301 р.) | ⬜ |
| 5 | 🧰 Інструменти | `screens/ComingSoon.tsx` — заглушка «Скоро» | ⬜ |
| 6 | ✅ Затвердження (тільки адмін) | `screens/Approval.tsx` (298 р.) | ⬜ |
| 7 | Внесення заднім числом | `screens/RetroEntry.tsx` (831 р.) | ⬜ |

Наскрізні елементи: `SyncStatusPill` (пігулка синку вгорі), тост «✅ Збережено»,
`BackRow`, `MainButton`, `NumericKeypad`.

### Кроки всередині Дорожнього табеля (`RoadTimesheet.tsx`)

| # | Крок | Рядок | Статус |
|---|------|-------|--------|
| 2.0 | `HUB` — хаб дня | 2299 | 🔧 |
| 2.1 | `PICK_CAR` — вибір авто | 2481 | 🔧 |
| 2.2 | `ODO_START` — одометр на виїзді | 2522 | 🔧 |
| 2.3 | `PICK_PEOPLE` — бригада | 2588 | 🔧 |
| 2.4 | `PICK_OBJECTS` — обʼєкти дня | 2760 | 🔧 |
| 2.5 | `PLAN` — план по обʼєктах | 2830 | 🔧 |
| 2.6 | `PLAN_WORKS` — роботи обʼєкта | 2865 | 🔧 |
| 2.7 | `PLAN_VOLUMES` — обсяги (список / ввід) | 2979 / 3088 | ⬜ |
| 2.8 | `READY` — перевірка перед виїздом | 3112 | 🔧 |
| 2.9 | `DRIVE` — у дорозі | 3213 | 🔧 |
| 2.10 | `ARRIVE_PICK` — куди приїхали (запасний вхід) | 3419 | ⬜ |
| 2.11 | `AT_OBJECT` — на обʼєкті (найбільший) | 3510 | 🔧 |
| 2.12 | `RETURN_PICKUP` — забір людей | 4275 | ⬜ |
| 2.13 | `RETURN` — повернення на базу | 4365 | ⬜ |
| 2.14 | `REVIEW` — підсумок перед відправкою | 4467 / 4491 | ⬜ |
| 2.15 | `DONE` — день відправлено | 2170 | ⬜ |

---

## Коло 1 — записи по екранах

> Заповнюється по ходу. Формат кожного запису — нижче.

<!--
### <номер> <Назва екрана>  — <статус>

**Файл:** `шлях:рядки`
**Куди веде:** …
**Що на екрані:** кнопки/поля списком
**Зауваження зі скріна (коло N):** …
**Мої поради:** …
**Зроблено:** … (коміт `abc1234`)
**Перевірити наступного кола:** …
-->

### 2.0 `HUB` — хаб Дорожнього табеля — 🔧

**Файл:** `apps/miniapp-web/src/screens/RoadTimesheet.tsx:2198`
**Куди веде:** Авто → `PICK_CAR`, Люди → `PICK_PEOPLE`, Обʼєкти → `PICK_OBJECTS`,
Роботи → `PLAN` (заблоковано без обʼєктів), «Внести день заднім числом» →
`RetroEntry`, головна кнопка → `READY`.

**Що на екрані:**
- Заголовок «🚗 Дорожній табель», рядок «ПОТОЧНА ПОЇЗДКА · дата» + «N/4 готово» і смужка прогресу
- Картка-пропозиція «Повторити останній виїзд?» — деталі, «Приховати», «Застосувати»
- Список налаштування дня: Авто · Люди · Обʼєкти · Роботи, у кожного бейдж стану
- «Внести день заднім числом · без таймерів ›»
- «🕐 Історія дня (N)»
- Головна кнопка «Далі → Перевірка перед виїздом» (неактивна, поки не 4/4)

**Зауваження зі скріна (коло 1):**
1. «Повторити маршрут з 2026-08-13?» — дата в заголовку зайва
2. Люди й роботи йшли суцільним текстом через кому — стіна тексту
3. Обʼєкти в деталях злипалися між собою
4. Підпис «Можна повертатись сюди у будь-який момент…» — зайвий

**Зроблено:**
1. Заголовок → «Повторити останній виїзд?». Дату не викинув, а переніс у рядок
   під ним: «2026-08-13 · Ауді · 5 людей · 2 обʼєктів» — інакше не видно, який
   саме день копіюємо
2. Люди й роботи — списком, кожен пункт з нового рядка з крапкою (новий клас
   `.bullets` в `index.css`)
3. Між обʼєктами відступ, назва обʼєкта окремим рядком над своїм списком робіт
4. Підпис прибрано

**Друга правка (коло 1, після повторного огляду хаба):**
- **«Авто: Ауді» вирівняно з рештою рядків.** У трьох із чотирьох назва була
  плаского типу («Люди», «Обʼєкти», «Роботи»), а значення — у бейджі; в авто
  значення сиділо в самій назві. Тепер скрізь однаково: назва «Авто», значення
  в бейджі
- **«200 км» підписано** — «Ауді · старт 200 км». Без слова цифра читалась як
  «проїхали 200 км»; це той самий привід, через який її прибрали з екрана
  вибору авто

**Перевірити наступного кола:** картка «Повторити останній виїзд?» — заголовок
без дати, люди й роботи списком з крапками, обʼєкти розділені; під блоком
Авто/Люди/Обʼєкти/Роботи більше немає сірого підпису.

**Мої поради — обидві прийнято й зроблено (див. «Наскрізні рішення»).**

---

### 2.1 `PICK_CAR` — вибір авто — 🔧

**Файл:** `apps/miniapp-web/src/screens/RoadTimesheet.tsx:2398`
**Куди веде:** тап по авто = вибір (без переходу), головна кнопка «Далі → Одометр» → `ODO_START`.
Назад → туди, звідки зайшли (`editReturnStep`: `HUB` або крок поїздки).

**Що на екрані:**
- Бейдж кроку «🚙 АВТО»
- Список авто: іконка 🚙 + назва + держномер, бейдж праворуч
- Зайняте авто — приглушене (opacity 0.4), не натискається, бейдж «🔒 Ім'я»
- Головна кнопка «Далі → Одометр» (неактивна, поки авто не обрано)

**Зауваження зі скріна (коло 1):** сам вибір ок, питання зручності.

**Зроблено:**
1. **Авто з минулого виїзду — першим** у списку, з сірим бейджем «минулого разу».
   Порядок довідника бригадиру нічого не каже, а те саме авто береться майже
   щодня. `lastTripCarId` тримається окремо від `lastTrip`, бо картку-пропозицію
   можна приховати або застосувати — а факт «цим їздили минулого разу» лишається
2. **Зайняті авто — вниз списку.** Раніше 🔒-авто стояло другим рядком, ловило
   тапи й розривало список
3. Прибрано заголовок «ВИБІР АВТО» — дублював бейдж «🚙 АВТО» над ним
4. Прибрано підказку «Обрано: X» — обране авто й так підсвічене в списку
5. Прибрано «177 км» (останній одометр) — на цьому екрані цифра ні на що не
   впливає, вона потрібна аж на наступному кроці (там і лишилась)

**Перевірити наступного кола:** авто минулого виїзду вгорі з бейджем «минулого
разу»; зайняті — в кінці; над списком лише бейдж «🚙 АВТО»; держномер сірий
(наслідок глобального `.hint`).

**Свідомо не робив:** для авто без пробігу нічого не показуємо (було б «новий») —
цифру з екрана прибрано взагалі, питання зняте.

**Не про дизайн, до даних (аркуш АВТО):**
- `TEST_AUTO AO2625IH` має **той самий держномер**, що й `Opel Movano AO2625IH`
- пробіг несумісний: Вольво 355586 км, Ауді 200 км — десь реальний одометр,
  десь лічильник із нуля. На перевірку «не менше попереднього» не впливає
  (порівняння в межах одного авто), але в БУХЗВІТ цифри підуть різнокаліберні

---

## Наскрізні рішення по дизайну

Сюди — правила, які ухвалили один раз і застосовуємо всюди (кольори, розміри
кнопок, формулювання, поведінка «Назад»), щоб не перевигадувати на кожному екрані.

### Вторинний текст сірий скрізь (коло 1)

`.hint` був описаний **тільки** всередині `.header`, тож усі ~130 інших
`className="hint"` по застосунку малювались як звичайний текст — того ж
розміру, ваги й кольору, що й контент, який вони мали підпирати. Тепер
`.hint` — глобальне правило: сірий, 14px, `line-height: 1.4`.

Винятки, які вже враховані:
- `.section-title .hint` успадковує розмір заголовка (13px), щоб лічильник
  «N/4 готово» не був більшим за сам заголовок
- підказки з інлайновим червоним кольором (попередження) не зачеплені —
  інлайн-стиль перебиває клас

**Це змінює вигляд усіх екранів одразу** — дивись уважно наступного кола.

### Числівники узгоджуються (коло 1)

Шаблони на кшталт `${n} робіт` завжди давали третю форму: «1 робіт», «2 робіт».
Новий `lib/plural.ts` дає українські три форми:

```ts
plural(n, "робота", "роботи", "робіт")   // одна / дві / пʼять
works(n) · people(n) · objects(n)         // готові лічильники з числом
```

Прогнано по 8 місцях у `RoadTimesheet.tsx` і `RetroEntry.tsx`. **Нові рядки з
числом і іменником пиши тільки через ці хелпери.**

### Бейджі: колір означає стан, а не «є значення / нема значення» (коло 1)

- сірий (просто `.badge`) — **ще не заповнено**, нормальний стан налаштування
- зелений (`.badge.ok`) — готово
- помаранчевий (`.badge.warn`) — треба увага: 🔒 зайнятий, «в процесі», «без годин»
- червоний (`.badge.danger`) — проблема

Три помаранчеві «не обрано» підряд у хабі читались як три помилки, хоча день
щойно почався. Переведено на сірий: `не обрано` (Авто/Люди/Обʼєкти), `спочатку
обʼєкти` та лічильник «N/M з роботами» в хабі, а також `не обрано` на екрані
`PLAN`. Помаранчевий лишається для справжніх перешкод.

---

## Відкладено (не робимо зараз)

_(поки порожньо)_

---

### 2.2 `ODO_START` — одометр на старті — 🔎

**Файл:** `apps/miniapp-web/src/screens/RoadTimesheet.tsx:2439`
**Куди веде:** «Зберегти» → назад туди, звідки зайшли (`editReturnStep`, зазвичай `HUB`).
Назад → `PICK_CAR`.

**Що на екрані:**
- Бейдж кроку «🚙 ОДОМЕТР НА СТАРТІ»
- Рядок з обраним авто (назва + держномер) — **додано цього кола**
- «Попереднє значення: N км (натисніть, щоб підставити)» — підкреслений текст-посилання
- Велика цифра «N км», під нею «+N км з попереднього виїзду» або червоне
  «⚠️ Не може бути менше за попередній приїзд»
- Цифрова клавіатура (`NumericKeypad`): 1-9, «.», 0, «Очистити», «⌫»
- «📷 Фото спідометра (не обовʼязково)» + системний файловий інпут
- Головна кнопка «Зберегти» (блокується, поки порожньо або значення менше попереднього).
  Тут же відбувається **резервування авто** (`reserveIfPossible`)

**Зауваження зі скріна (коло 1):** не було видно, до якого авто цей одометр.

**Зроблено:** над клавіатурою рядок з обраним авто. Заодно злито два заголовки
(«🚙 АВТО · ОДОМЕТР» + «ОДОМЕТР НА СТАРТІ») в один бейдж — рядок з авто зайняв
місце, що звільнилось.

**Мої поради — усі 5 прийнято й зроблено:**
1. **Кнопка фото більше не системна.** Було `Choose File / No file chosen` —
   єдиний елемент англійською, без стилів. Новий компонент
   `components/PhotoButton.tsx` ховає інпут за нормальною кнопкою «📷 Зняти
   спідометр». Замінено в **усіх трьох** місцях: одометр на старті, одометр на
   фініші, фото обʼєкта («📷 Додати фото», без `capture` — там доречно і з галереї)
2. **Крапка на клавіатурі одометра прибрана** (`NumericKeypad decimal={false}`).
   Одометр — цілі км, а «.» сусідить із «0» і легко тиснеться помилково.
   Застосовано і до одометра на фініші, і до одометра роз'їздів. Для обсягів
   робіт і ручних годин крапка лишилась — там вона потрібна
3. **Порожнє «0 км» тепер сіре й напівпрозоре** (`.big-number.empty`) — видно,
   що це заглушка, а не введений нуль. Застосовано до всіх великих цифр:
   одометри, обсяг роботи, ручні години, роз'їзди
4. **«Попереднє значення… (натисніть, щоб підставити)»** → кнопка-чіп
   «↩︎ Підставити 200 км». Це найчастіша дія на екрані, а виглядала як примітка
5. **«🗑 Скинути день»** — підтвердження вже було (`confirmDialog` у `resetDay`),
   але сама кнопка малювалась на **кожному кроці**, зокрема в дорозі й на
   обʼєкті під час зміни. Тепер лише на `HUB`, куди й так один тап назад

**Перевірити наступного кола:** видно назву авто й держномер над клавіатурою;
кнопка «📷 Зняти спідометр» замість `Choose File`; на клавіатурі одометра немає
крапки (ряд із трьох кнопок); порожнє «0 км» сіре; чіп «↩︎ Підставити 200 км»;
«🗑 Скинути день» видно тільки в хабі.

---

### 2.3 `PICK_PEOPLE` — люди в поїздці — 🔧

**Файл:** `apps/miniapp-web/src/screens/RoadTimesheet.tsx:2510`
**Куди веде:** «Зберегти» → назад (`editReturnStep`, зазвичай `HUB`). Назад → `HUB`.

**Що на екрані:**
- Бейдж «👥 ЛЮДИ», заголовок «Люди в поїздці»
- Панель обраних (`.picked-panel`) — «▸ Обрано N» + «🗑 Очистити», у розгорнутому
  вигляді список імен, у кожного ✕
- Пошук людини
- Список бригад (`groupByBrigade`), у кожної бейдж «обрано/всього»; розкривається
  одна бригада за раз, при пошуку — всі
- Усередині бригади: «✓ Обрати всю бригаду» + рядки людей (чекбокс, аватар з
  ініціалами, ім'я, тег ролі або «🔒 хто зайняв»)
- Головна кнопка «Зберегти» (неактивна, поки нікого не обрано)

**Зауваження зі скріна (коло 1):** бригадира вгору, зайнятих вниз, обраних —
списком із хрестиками, кнопку «Очистити вибір» причепурити, все під один стиль.

**Зроблено:**
1. **Порядок усередині бригади: бригадир → старший садівник → робітники**, далі
   за алфавітом (`lib/employee.ts`, `byRoleThenName`). Раніше було чисто за
   алфавітом, і двоє людей, яких шукають першими, губилися в середині списку.
   Це зачіпає **всі** екрани, що групують по бригадах
2. **Зайняті — вниз своєї бригади** (сортування в екрані, після рольового —
   стабільне, тож ролі всередині зберігаються). Їх однаково не можна обрати
3. **Панель обраних** замість рядка через кому: кожен обраний окремим рядком зі
   своїм ✕. Раніше, щоб зняти одну людину, треба було знайти її назад у бригаді
4. **«🗑 Очистити»** — тепер той самий червоний текстовий стиль, що й
   «🗑 Скинути день», і живе всередині панелі обраних, а не в заголовку
5. **Той самий блок застосовано до `PICK_OBJECTS`** — обидва вибори тепер
   виглядають і працюють однаково

**Мої поради — усі 5 прийнято й зроблено:**
1. **Бейдж «🔒 …» скорочено** до «Прізвище І.» (`surnameInitial`). Повне
   «🔒 Левченко Альбіна Сергіївна» ламало рядок людини навпіл. Те саме на
   екрані вибору авто
2. **Лічильник бригади рахує доступних**: було «0/8», хоча троє зайняті іншим
   бригадиром і обрати можна лише 5 — «Обрати всю бригаду» назавжди застрягало
   на 5/8, ніби щось недороблено. Тепер «0/5», а поруч сіре «3 зайнято»
3. **Попередження, якщо в поїздці немає бригадира** — його 20% не
   нараховуються, робітники ділять 90%. Це свідомий вибір, але досі він був
   невидимий: ні тут, ні на «Перевірці перед виїздом» про це не було ні слова
4. **Аватар з ініціалами прибрано** зі списку вибору: «ДМ» перед «Дидик Максим
   Романович» не додавав нічого, а з'їдав ширину, через що ім'я й тег ролі
   тулилися. В інших місцях (на об'єкті) аватар лишився — там він доречний
5. **Імена скорочено** до «Прізвище Ім'я І.» (`shortName`) у списку вибору й у
   панелі обраних. Повні імена лишились у підсумку дня та у звіті

**Заодно:** той самий вигляд рядка людини застосовано в `Logistics.tsx` —
інакше наступного кола довелось би переробляти те саме вдруге.

**Перевірити наступного кола:** бригадир першим у бригаді; зайняті в кінці;
«▸ Обрано N» розгортається у список із ✕; «🗑 Очистити» червоним; на екрані
обʼєктів — та сама панель; «🔒 Левченко А.» замість повного імені; «0/5» +
«3 зайнято»; попередження про відсутнього бригадира; без аватарів у списку.

---

### 2.4 `PICK_OBJECTS` — обʼєкти маршруту — 🔧

**Файл:** `apps/miniapp-web/src/screens/RoadTimesheet.tsx:2668`
**Куди веде:** «Зберегти» → `HUB`. Назад → `HUB`.

**Що на екрані:** те саме, що й у людей, але групування по місту/адресі
(`groupByCity`): панель обраних із ✕ → пошук → групи міст → «✓ Обрати всі в
місті» + рядки обʼєктів.

**Зауваження зі скріна (коло 1):** сам вибір ок, звести стилі до вигляду людей.

**Зроблено:**
1. **Іконку 📍 прибрано** з рядків — вона була однакова в усіх, як і аватар у
   людей
2. **Адресу праворуч прибрано.** `groupByCity` групує **саме за адресою**, тож
   праворуч у кожному рядку стояла точна копія заголовка групи, під якою рядок
   і був. Завжди, без винятків
3. **🏙 з заголовка групи прибрано** — у бригад емодзі немає, тепер однаково
4. Панель обраних із ✕ і червоне «🗑 Очистити» вже стояли (коло 1, крок 2.3)

**Перевірити наступного кола:** рядок обʼєкта = чекбокс + назва, без іконки й
без адреси; заголовок міста без 🏙; панель «▸ Обрано N» з ✕.

---

### 2.5 `PLAN` — роботи на обʼєктах (список обʼєктів) — 🔧

**Файл:** `apps/miniapp-web/src/screens/RoadTimesheet.tsx:2765`
**Куди веде:** тап по обʼєкту → `PLAN_WORKS`, 🗑 → прибрати з маршруту,
«Зберегти» → `HUB`.

**Що на екрані:** бейдж «🧱 РОБОТИ», заголовок, підказка, список обʼєктів
(назва + бейдж «N робіт» / «не обрано» + 🗑).

**Зроблено:**
1. Іконку 📍 з рядків прибрано — як в інших пікерах
2. **🗑 тепер питає підтвердження** («Прибрати "X" з маршруту?»), і якщо на
   обʼєкті вже обрані роботи — каже, скільки їх зникне. Раніше один тап
   мовчки зносив обʼєкт разом з усім планом

---

### 2.6 `PLAN_WORKS` — вибір робіт на обʼєкті — 🔧

**Файл:** `apps/miniapp-web/src/screens/RoadTimesheet.tsx:2803`
**Куди веде:** «Готово» → `worksReturnStep` (`PLAN` або крок поїздки).

**Що на екрані:**
- Бейдж «НАЗВА ОБʼЄКТА · РОБОТИ»
- Панель обраних із ✕ і «🗑 Очистити»
- Підказка «Обери роботи. Обсяги вкажете пізніше…», пошук
- Категорії (`groupWorks`) → **підкатегорії** → роботи; у кожної групи
  «✓ Обрати всі в категорії/підкатегорії»
- Нотатки, фото обʼєкта
- Головна кнопка «Готово»

**Зроблено:**
1. **Підкатегорії тепер видно як окремий рівень** — новий клас `.subcat-cell`:
   лінія по лівому краю, дрібніший сірий напівжирний текст. Раніше і категорія,
   і підкатегорія були однаковими рядками з трикутником і лічильником
2. **Підкатегорії підняті на початок категорії**, роботи без підкатегорії — під
   ними. Раніше було навпаки, і купа «вільних» робіт ховала підкатегорії десь
   унизу. Порядок усередині лишається алфавітним (`lib/works.ts`)
3. **Панель обраних** зі списком і ✕ на кожній роботі, «🗑 Очистити» червоним —
   як у людей і обʼєктів
4. Прибрано дубль «Робіт у пакеті: N» унизу — та сама цифра вже стоїть у
   «Обрано N» угорі

5. **Фото обʼєкта прибрано з цього екрана** (за рішенням: тут воно не потрібне).
   Разом із блоком пішли `uploadObjectPhoto`/`removeObjectPhoto` — вони існували
   тільки заради нього, а `noUnusedLocals` не дає лишити мертвий код.
   **Наслідок:** фото обʼєкта тепер не додається ніде. Поле `photoUrls` і далі
   їде на сервер, просто завжди порожнє. Якщо фото знадобиться — його місце на
   екрані `AT_OBJECT`, де бригадир реально стоїть на обʼєкті
6. **Підказку «Обери роботи. Обсяги вкажете пізніше…» прибрано** — обсяги тут
   не вводяться, а «обери роботи» вже сказано заголовком
7. **«✓ Обрати всі в категорії» питає підтвердження** («Додати всі N робіт на
   обʼєкт?») — але лише коли додається **10+** робіт. Зняття вибору не питає:
   воно оборотне одним тапом. Малі підкатегорії (5-12) лишаються швидкими
8. **Нотатки лишились** без змін
9. Пошук лишився там, де стояв — над списком

**Перевірити наступного кола:** підкатегорії з лінією зліва й сірим текстом,
одразу під заголовком категорії; панель «▸ Обрано N» з хрестиками; 🗑 на екрані
`PLAN` питає підтвердження; на екрані робіт немає блоку фото; «Обрати всі в
категорії» на великій категорії питає підтвердження.

---

### 2.8 `READY` — перевірка перед виїздом — 🔧

**Файл:** `apps/miniapp-web/src/screens/RoadTimesheet.tsx:3046`
**Куди веде:** ✏️ біля авто → `PICK_CAR`, біля людей → `PICK_PEOPLE`, біля
обʼєкта → `PLAN_WORKS`; усі повертаються **сюди ж** (`editReturnStep`/
`worksReturnStep` = `READY`). Назад → `HUB`. «🚗 Виїхати» → `startDrive` → `DRIVE`.

**Що на екрані:**
- Бейдж «ПЕРЕВІРКА ПЕРЕД ВИЇЗДОМ», заголовок «Готовність до виїзду»
- Рядок «Авто» + «Ауді · старт 200 км» + ✏️
- Рядок «Люди» + лічильник + ✏️, розгортається у список імен
- «ОБʼЄКТИ · РОБОТИ»: кожен обʼєкт розгортається у список робіт, у кожного ✏️
- Головна кнопка «🚗 Виїхати»

**Перевірено вручну:** ✏️ на всіх трьох рядках повертає на цей самий екран,
кнопка «Назад» зверху — теж.

**Зауваження зі скріна (коло 1):** «Щось треба змінити? ← До меню поїздки» —
зайве, бо «Назад» веде рівно туди ж.

**Зроблено:**
1. Рядок «Щось треба змінити?…» прибрано — `backTargets` для `READY` це вже `HUB`
2. Списки людей і робіт переведено на спільний `.bullets`. Були вручну набрані
   «•» у `div`, через що довга назва роботи переносилась **під крапку**, а не
   з відступом
3. «Ауді · 200 км» → «Ауді · старт 200 км» — те саме формулювання, що й у хабі
4. 📍 з рядків обʼєктів прибрано — під заголовком «ОБʼЄКТИ» вона однакова в усіх

**Мої поради — прийнято й зроблено:**
5. **Ролі в списку людей.** Тег «бригадир» / «старший» поруч з іменем; для
   робітників тега немає — він повторювався б у кожному рядку. Список
   відсортовано по ролі, як у пікері
6. **Попередження про відсутнього бригадира продубльовано сюди.** На вибір
   людей заходять на початку і швидко, а цей екран — остання брама перед
   виїздом, його читають уважно

**Перевірити наступного кола:** немає рядка «Щось треба змінити?»; крапки в
списках вирівняні з висячим відступом; «старт 200 км»; без 📍 біля обʼєктів;
бригадир першим у списку з тегом ролі; попередження, якщо бригадира немає.

---

### 2.9 `DRIVE` — у дорозі — 🔧

**Файл:** `apps/miniapp-web/src/screens/RoadTimesheet.tsx:3149`
**Куди веде:** «✏️ Редагувати поїздку» → `HUB` (перевірено вручну: редагуєш,
зберігаєш, повертаєшся в поїздку). «📍 Прибув: X» → `AT_OBJECT`. Коли всі
обʼєкти відвідано → `RETURN_PICKUP` або `RETURN`.

**Що на екрані:** таймер часу в дорозі (лише дорога, обʼєкти не рахуються),
вибір/назва пункту призначення, «По дорозі» (забрати/висадити), список
маршруту з бейджами людей і робіт, головна кнопка.

**Зроблено:**
1. **Пункт призначення тепер обирається, а не вгадується.** Було «Прямуємо до
   📍 X», де X — просто **перший невідвіданий обʼєкт зі списку**; насправді
   ніхто не знав, куди їде машина. Тепер після «Виїхати» (і після кожного
   обʼєкта) екран питає «Куди їдемо?» списком невідвіданих, а далі показує
   обраний пункт із кнопкою «змінити»
2. **«Прибув на обʼєкт» фіксує саме той обʼєкт** — «📍 Прибув: Апартель
   Східниця 3 черга». Проміжний екран «На який обʼєкт ви прибули?»
   (`ARRIVE_PICK`) лишився запасним виходом під посиланням «Прибули на інший
   обʼєкт?» — на випадок, коли доїхали не туди, куди збиралися
3. **`headingToObjectId` зберігається в чернетці** — якщо Telegram вивантажить
   застосунок посеред дороги, пункт призначення не загубиться. Він
   автоматично «згасає», якщо обʼєкт уже відвідали або прибрали з маршруту
4. **«Висадити/забрати по дорозі» — компактний список по бригадах** замість
   килима з чіпів (новий `MiniPeopleList` + `.mini-list` у CSS). Щільніше за
   екрани налаштування — його відкривають на ходу, щоб торкнутися однієї
   людини, а не планувати день. Показуються **тільки вільні**: без тих, хто вже
   в машині, стоїть на обʼєкті або зайнятий іншим бригадиром. Імена скорочені

5. **«Куди їдемо?» показує весь маршрут**, а не лише невідвідане. Відвідані
   позначені ✅ і бейджем «вже були», але лишаються доступними — повернутись
   на обʼєкт удруге це нормальний день. Заодно зі списку видно, що вже пройдено
6. **Зміна напрямку в дорозі** — кнопка «✏️ їдемо в інше місце» біля пункту
   призначення повертає той самий список маршруту, де можна вибрати інший
   обʼєкт. Саме це й треба, коли по дорозі вирішили спершу заскочити в інше
   місце — **порядок обʼєктів у маршруті ні на що не впливає**, це просто
   список на день

   > Спершу я зробив тут «✏️ Змінити маршрут» із переходом на екран вибору
   > обʼєктів — це було не те: маршрут (склад обʼєктів) у дорозі не редагують,
   > змінюється лише напрямок. Відкотив разом із `objectsReturnStep`.

**Перевірити наступного кола:** після «Виїхати» — питання «Куди їдемо?» з усіма
обʼєктами і ✅ біля відвіданих; головна кнопка називає обраний обʼєкт;
«✏️ їдемо в інше місце» повертає до вибору напрямку;
«Висадити/забрати» — список по бригадах, а не чіпи.

**Наступне на цьому екрані (домовлено):** підказка «🚶 біля обʼєкта — додати
тих, хто вже приїхав туди своїм ходом, і почати їм роботи, поки ви ще в
дорозі» і сам сценарій раннього самостійного прибуття.

---

### 2.11 `AT_OBJECT` — на обʼєкті — 🔎 (коло 1, частина зроблена)

**Файл:** `apps/miniapp-web/src/screens/RoadTimesheet.tsx:3493`

Екран має **два стани**, і це головне, що про нього треба знати:
- `carPresent = !drivingSegmentStartedAt` — машина реально тут
- інакше («🚗 МАШИНА ЩЕ В ДОРОЗІ») — екран відкрито з дороги кнопкою 🚶, щоб
  записати тих, хто дістався своїм ходом, і стартувати їм роботи

**Зроблено (коло 1):**
1. Панель обраних у списку «Хто приїхав сам» + спільний вигляд рядка
2. Приховано, коли машина в дорозі: «Ввести години вручну», «Машина вибула по
   справам», «Перенести людей на інший обʼєкт»
3. **Два блоки людей злито в один** — «Люди на обʼєкті». Раніше ті самі люди
   були і в «Люди тут — кожен зі своїм таймером» (таймери), і в «Як люди їдуть
   з обʼєкта» (посадити/зняти), у різному порядку, з половиною дій у кожному.
   Тепер один рядок: маркер 🚶/🚐 (як дістався сюди — від цього залежить, чи
   доступний бус), імʼя, таймер, ▶️/⏹, і під ним чіпи «🛠 Призначити окремі
   роботи», «🚐 У бус», «🚶 Зняти з обʼєкта». Масові дії — під списком
4. **Призначення робіт з боку людини.** Та сама сутність, що й «👥 вся бригада»
   з боку роботи (`w.employeeIds`), просто другий вхід. Під іменем видно
   «🛠 окремо: …», а в списку робіт видно, кому робота вже належить
5. **Попередження про закріплені роботи** — бурштиновий блок на екрані
   («🛠 Закріплені роботи: Полив газону — Борисова В.») + діалог при
   загальному «Почати роботи»: «За них платять лише цим людям — бригада їх не
   отримає. Почати роботи решті?»

**Важливе про механіку (з`ясовано під час роботи):**
- **Таймери біля робіт у розрахунок не йдуть.** У payload від роботи їде лише
  `volume` і `employeeIds`; гроші рахуються з **сесій людей**. Тобто ▶️ біля
  роботи — просто секундомір, ▶️ біля людини — гроші
- **Закріплена робота НЕ забирає в людини частку спільного кошика.** У
  `payroll.ts`: `pay = sharedOnePay + dedicatedPay`. Тобто зараз людина
  отримує і свою окрему роботу, **і** рівну частку решти. Чи має бути інакше —
  відкрите питання до власника (див. нижче)

**Питання про гроші — вирішено, формулу змінено** (`packages/core/src/payroll.ts`):

Фонд об'єкта — це **сума його робіт** (`обсяг × тариф`), окремої «ціни
об'єкта» ніде немає (`routes/roadTimesheet.ts:170`).

Було: `pay = sharedOnePay + dedicatedPay` — людина із закріпленою роботою
отримувала і свою роботу, **і** повну рівну частку решти.
Стало: `pay = (у спільному поділі ? sharedOnePay : 0) + dedicatedPay` —
**бригада отримує за свої роботи, закріплена людина за свою**.

Приклад: полив 2000 (Борисова) + інші роботи 8000, 5 робітників, бригадира
немає (робітники ділять 90%):

| | було | стало |
|---|---|---|
| Борисова | 1800 + 1440 = **3240** | **1800** |
| решта ×4 | по **1440** | по **1800** |

**Запобіжник:** якщо закріплена робота є в **усіх** присутніх робітників,
спільний залишок ділиться між усіма ними, а не зникає — гроші з об'єкта не
щезають ніколи.

`accounting.ts` міняти не довелось: він розкидає вже пораховану виплату по
роботах, на яких людина позначена, — з новим правилом це стало точнішим.

---

# Коло 2

Перше коло пройдено до кроку `AT_OBJECT` включно; `RETURN_PICKUP`, `RETURN`,
`REVIEW`, `DONE` і всі екрани поза табелем (Меню, Логістика, Матеріали,
Статистика, Затвердження, Ретро) — ще не дивилися жодного разу.

Друге коло починаємо **з початку**: заодно перевіряємо все, що виправили.

## Що має бути видно після перезавантаження

**Наскрізь по всіх екранах:**
- [ ] вторинний текст (підказки) сірий і дрібніший — глобальний `.hint`
- [ ] сірі бейджі «не обрано» замість помаранчевих; помаранчевий лишився для
      🔒 зайнятий / «в процесі» / «без годин»
- [ ] числівники узгоджені: «1 робота», «2 роботи», «9 робіт», «2 обʼєкти»
- [ ] імена в списках скорочені: «Левченко Роман М.»; повні — у підсумках

**2.0 Хаб:** «Повторити останній виїзд?» без дати в заголовку (дата в рядку
під ним) · люди й роботи списком з крапками · обʼєкти розділені · немає
підпису «Можна повертатись сюди…» · рядок «Авто» + бейдж «Ауді · старт 200 км»

**2.1 Вибір авто:** авто минулого виїзду перше з бейджем «минулого разу» ·
зайняті в кінці · тільки бейдж «🚙 АВТО» над списком · держномер сірий · без
колонки км

**2.2 Одометр:** видно назву авто й держномер · кнопка «📷 Зняти спідометр»
замість `Choose File` · на клавіатурі немає крапки · порожнє «0 км» сіре ·
чіп «↩︎ Підставити 200 км» · «🗑 Скинути день» тільки в хабі

**2.3 Люди:** бригадир перший у бригаді · зайняті в кінці · «▸ Обрано N»
розгортається у список із ✕ · «🗑 Очистити» червоним · «🔒 Левченко А.» ·
лічильник «0/5» + «3 зайнято» · попередження, якщо бригадира немає · без
аватарів

**2.4 Обʼєкти:** рядок = чекбокс + назва (без 📍 і без адреси праворуч) ·
заголовок міста без 🏙 · панель «▸ Обрано N» з ✕

**2.5 / 2.6 Роботи:** 🗑 біля обʼєкта питає підтвердження · підкатегорії з
лінією зліва й сірим текстом, одразу під заголовком категорії · панель обраних
із ✕ · немає блоку фото · «Обрати всі в категорії» питає підтвердження від 10 робіт

**2.8 Перевірка перед виїздом:** немає «Щось треба змінити?» · крапки з висячим
відступом · «старт 200 км» · без 📍 · бригадир першим із тегом ролі

**2.9 У дорозі:** після «Виїхати» — «Куди їдемо?» з усіма обʼєктами і ✅ біля
відвіданих · головна кнопка називає обраний обʼєкт · «✏️ їдемо в інше місце» ·
«Висадити/забрати» — компактний список по бригадах

**2.11 На обʼєкті:** один блок «Люди на обʼєкті» замість двох · маркер 🚶/🚐 ·
чіпи «🛠 Призначити окремі роботи» / «🚐 У бус» / «🚶 Зняти з обʼєкта» ·
бурштиновий блок «Закріплені роботи» · коли машина в дорозі — немає «Ввести
години вручну», «Машина вибула по справам», «Перенести людей»

## Ще не бачили жодного разу
2.7 `PLAN_VOLUMES` · 2.10 `ARRIVE_PICK` · 2.12 `RETURN_PICKUP` ·
2.13 `RETURN` · 2.14 `REVIEW` · 2.15 `DONE` · Меню · Логістика · Матеріали ·
Статистика · Затвердження · Ретро-внесення

## Записи кола 2

_(порожньо — починаємо)_
